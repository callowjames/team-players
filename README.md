# team-players
Site using PHP to dynamically create HTML objects with data from a database and to view and maintain table data. Uses JQuery to to manipulate web page content and validate user input. Uses AJAX to improve the dynamic capabilities.
